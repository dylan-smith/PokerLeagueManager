<UserControl x:Class="PokerLeagueManager.UI.Wpf.Views.PlayerGamesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             mc:Ignorable="d" 
             MinHeight="100" MinWidth="100" d:DesignWidth="300" d:DesignHeight="400">
    <Grid Margin="0,0,0,0">
        <Label Content="Player Name:" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Height="25"/>
        <TextBox x:Name="PlayerNameTextBox" Text="{Binding PlayerName, FallbackValue='Hulk Hogan'}" HorizontalAlignment="Stretch" Height="23" Margin="93,10,10,0" TextWrapping="NoWrap" VerticalAlignment="Top" IsReadOnly="True" Background="#FFDDDDDD" VerticalContentAlignment="Center" />
        <ListBox x:Name="GamesListBox" ItemsSource="{Binding Games}" SelectedIndex="{Binding Path=SelectedGameIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" Margin="10,50,10,60" VerticalAlignment="Stretch"/>
        <Button x:Name="CloseButton" Command="{Binding CloseCommand}" Content="Close" HorizontalAlignment="Stretch" Margin="10,0,10,10" Height="40" VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>
