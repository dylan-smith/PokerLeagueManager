<UserControl x:Class="PokerLeagueManager.UI.Wpf.Views.ViewGamesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MinHeight="100" MinWidth="100" d:DesignWidth="300" d:DesignHeight="300">
    <Grid>
        <Button Command="{Binding AddGameCommand}" Content="Add Game" HorizontalAlignment="Stretch" Margin="10,10,10,0" VerticalAlignment="Top" Height="40"/>
        <ListBox ItemsSource="{Binding Games}" HorizontalAlignment="Stretch" Margin="10,60,10,10" VerticalAlignment="Stretch" />
    </Grid>
</UserControl>
